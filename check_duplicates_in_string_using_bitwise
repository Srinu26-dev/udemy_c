//Bitwise operations
//Left shift <<
//Bits ORing (merging)
//BIts ANDing(masking)

#include<stdio.h>
int main()
{
char A[]="finding";

int H=0,x=0 ,i=0;
for(i=0;A[i]!='\0';i++)
{
  x=1;
 x=x<<(A[i]-97);
if((x&H) >0)
{
 printf("%c is Duplicate\n",A[i]);
}
else
{
 H=x|H;
}
}
}
